# Splearn 도메인 모델

## 도메인 모델 만들기
1. 듣고 배우기
2. '중요한 것'들 찾기 (개념 식별)
3. '연결 고리' 찾기 (관계 정의)
4. '것'들을 설명하기 (속성 및 기본 행위 명시)
5. 그려보기 (시각화)
6. 이야기 하고 다듬기 (반복)

## Splearn 도메인

## 도메인 모델
### 회원
엔티티
도메인을 모델을 만들때 사용하는 패턴
도메인 안에 있는 대상이나 개념
고유한 식별자를 가지고 이를 통해 개별적으로 구분됨
생명주기를 가진다.



#### _entity_
_Aggregate Root_

#### 속성
- `id`: 아이디 long (`AbstractEntity`)
- `email`: `Email` 이메일 - 자연키(`@NaturalId`)
- `nickname`: 닉네임
- `passwordHash`: 비밀번호
- `status`: `MemberStatus` 회원 상태
- `detail`: `MemberDetail` 1:1
#### 행위
- `static register()`: 회원 등록: `MemberRegisterInfo`, `PasswordEncoder`
- `activate`: 등록을 완료시킨다.
- `deactivate`: 탈퇴시킨다.
- `verifyPassword()`: 비밀번호를 검증한다.
- `changeNickname()`: 닉네임을 바꾼다.
- `changePassword()`: 비밀번호를 바꾼다.
- `updateInfo()`: 닉네임/프로필/자기소개를 수정한다.
- `isActive()`: 등록 완료 상태인지 확인한다.
#### 규칙
- 회원 생숭 후 등록대기
- 일정조건을 만족하면 등록 완료가 된다.
- 등록 완료된 상태에서만 탈퇴할 수 있다.
- 등록 대기 상태에서만 등록 완료가 될 수 있다.
- 등록 완료 상태에서만 정보를 수정할 수 있다.

### 회원상태
_Enum_
#### 상수
- `PENDING`: 등록대기
- `ACTIVE`: 등록완료
- `DEACTIVATED`: 탈퇴

### 회원 상세 (MemberDetail)
_entity_
`Member` 애그리거트에 속하며 애그리거트 루트를 통해서만 변경한다. (메서드 package-private)

#### 속성
- `id`: `Long` (`AbstractEntity`)
- `profile`: `Profile` 프로필 주소
- `introduction`: 자기 소개
- `registeredAt`: 등록 일시
- `activatedAt`: 등록 완료 일시
- `deactivatedAt`: 탈퇴 일시

#### 행위
- `static create()`: 회원등록. 현재 시간을 등록 일시로 저장한다.
- `setActivatedAt()`: 등록 완료 일시를 기록한다.
- `deactivate()`: 탈퇴 일시를 기록한다.
- `updateInfo()`: 프로필 주소, 자기 소개를 수정한다.

#### 규칙
- 등록 완료 일시, 탈퇴 일시는 한 번만 기록할 수 있다.
- 프로필 주소가 비어 있으면 프로필을 갖지 않는다.

### 이메일 (Email)
_Value Object_ (`domain.shared`)
- `address`: 이메일 주소
#### 규칙
- 이메일 형식이어야 한다.

### 프로필 (Profile)
_Value Object_
- `value`: 프로필 주소
#### 행위
- `url()`: `@` + 프로필 주소
#### 규칙
- 영소문자와 숫자만 사용한다.
- 최대 15자리를 넘을 수 없다.

### 요청 값 (record)
- `MemberRegisterInfo`: email, nickname(5~20), password(8~100)
- `MemberInfoUpdateRequest`: nickname(5~20), profileAddress(최대 15), introduction

### DuplicateEmailException
_Exception_

### 비밀번호 인코더 (PasswordEncoder)
_Domain Service_
#### 행위
- `encode()`: 암호화 하기
- `matches()`: 비밀번호가 일치하는지 확인
필요한 시점에 멤버라는 엔터티에 집어넣어 준다.
메소드 호출 주입.
외부 도메인 서비스를 주입할때 정적 팩토리 메서드를 주로 사용하기도 한다.

### 강사
### 강의
### 수업
### 섹션
### 수강
### 진도
(미구현)


## 도메인 모델 패턴
- 도메인/비즈니스 로직을 구성하는 아키텍처 패턴의 한가지
- 도메인 모델의 속성과 행위를 모두 포함하는 도메인의 오브젝트 모델이다.
- 오브젝트 모델이기 때문에 복잡한 연관관계, 커스텀 속성, 상소 등을 사용할 수 있다.
- 트랜잭션 스크립트는 하나의 업무절차를 처리하기 위한 스크립트를 만들고 비즈니스 로직을 순서대로 코드로 작성하는 방법
- 단순 도메인 모델: DB 설계와 유사, 테이블에 대해 하나의 도메인 오브젝트
- 풍성한 도메인 모델: 상속, 전략, GoF 디자인 패턴, 연관관계 복잡한 로직에 적합하지만 DB 매핑이 어려울 수 있다.
- 도메인 모델 패턴은 트랜잭션 스크립트 처럼 작업 단위의 절차형 API를 만들기 어렵다.
- 도메인 로직의 명확한 작업 단위 API를 제공하는 애플리케이션 서비스가 필요

## 값 객체
- 도메인 모델에서 식별자가 필요하지 않고 속성/값으로만 구별되는 오브젝트
- 엔티티가 너무 많은 책임을 가지는 것을 방지하고 특정 속성 관련 행위를 분리해서 엔티티를 더 집중된 상태로 유지하게 한다
- 원시 타입보다는 도메인 개념을 더 명시적을 나타내서 모델의 명확성을 높힌다
- 생성 이후에 상태가 변하지 않고 변경이 필요하면 새로운 객체로 교체한다.
- 풍부한 기능을 가진다.
- 자체 유효성 검사도 가능하다.
- 값 객체는 도메인 모델의 핵심 구성 요소로 값 그 자체의 의미를 담아내고 불변성, 동등성 등의 도메인 로직을
포함하여 설계하는 방식

### [강사 애그리거트]
애그리거트간 의존관계는 단방향이어야한다
### 강사 (Instructor)
_Aggregate Root_
#### 속성
- `id`: `long`,
- `member`: `Member`
- `status`

#### 행위
- `static apply(member)`: 강사 신청
- `approve()`: 강사 신청 승인
- `reject()`: 강사 신청 거절
- `isActive()`: 승인 완료 상태
- `ensureActive()`: 승인 완료를 보장

#### 규칙

### 강사 상태
_Enum_
#### 상수
- `PENDING`: 등록대기
- `ACTIVE`: 등록완료
- `REJECTED`: 탈퇴
