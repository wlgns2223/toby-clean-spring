- 엔티티를 클라이언트로 리턴해도 괜찮다.

# 엔티티 식별자
- 고유성: 두개의 엔티티가 같은 값을 가지면 안된다.
- 불변성: 한번 값이 할당되면 엔티티의 생명주기 동안 절대 변경되면 안된다.
- 비즈니스적인 의미로부터 디커플링 되는 것이 낫다. (자연키)
- 인조키(대리키,surrogate key)사용하는것이 낫다.

# 도메인 엔티티 (Member)
- `@NaturalId`: hibernate 지원, unique 조건 걸어줌
- `@NaturalIdCache`: 같은 트랜잭션안에서 같은 아이디로 읽을때 영속 컨텍스트안에서 읽어옴. 이메일로 읽어올때도.

# 애플리케이션 서비스
- 애플리케이션 서비스는 어떤 절차를 따르는지 코드에 잘 드러나야한다. 잘 읽혀야한다.
- 이걸 읽을때 기술적인 내용 <-> 비즈니스 로직적인 내용으로 관심사가 왔다갔다 한다면 리팩토링이 필요하다. 코드가 잘 안읽힌다는 뜻임.
- 회원 등록 절차 예: 검사 → 도메인 모델 생성 → repository 저장 → 후처리 작업
- 이메일을 보낼때 첫번째 파라미터는 뭐고..제목을 어떻게 보내고..는 디테일한 내용이다.
- 디테일은 숨기고 최상위 public method는 문서처럼 읽혀야한다.

# 테스트
- 임베디드 DB를 사용해서 애플리케이션 서비스 레이어 테스트를 하자
- 매뉴얼 테스트보다 스프링 컨테이너를 띄우는 쪽이 테스트하기가 더 쉽다.
- `@Transactional`을 붙이면 테스트를 수행하는 동안에 데이터베이스에 했던 모든 작업을 테스트가 끝날때 성공이든 실패든 상관없이 롤백을 시켜줌
  - `Classes annotated with '@Transactional' could be implicitly subclassed and must not be final` 위 에러는 무시. 버그임
- Test작성시 class에 autowired가 너무 많으면 보기 힘드니, JUnit에 따르면 간결하게 record로 작성해도 되지만, Bean 주입 방법을 명시해줘야함
  - `@TestConstructor(autowireMode = TestConstructor.AutowireMode.ALL)`
- Mock 테스트 방식
  - Stub: 호출 시 미리 정해진 값만 반환
  - Mock(수동): interaction(호출 여부, 인자) 검증까지 수행
  - Mockito: `Mockito.mock(...)` + `Mockito.verify(...)`로 간결하게 mock interaction 검증
- 매뉴얼 Mock 테스트는 결국 통합 테스트(`@SpringBootTest` + `@Transactional`)로 대체되었음. `SplearnTestConfiguration`에서 가짜 `EmailSender`/`PasswordEncoder` 빈을 주입해 컨텍스트 비용을 낮춤.
- 검증 제약(`@Size`, `@Email`)을 추가하면 기존 테스트 픽스처도 같이 따라가야 함. (예: password `"secret"` → `"verysecret"`, nickname `"Toby"` → `"Charlie"`)

# 헥사고날 - 서비스 분리 (CQS)
- 하나의 `MemberService`를 책임별로 분리: `MemberQueryService`(조회) + `MemberModifyService`(변경).
- 변경 서비스가 조회 포트(`MemberFinder`)를 그대로 의존성으로 주입받아 재사용 가능.
  - "단순히 조회 로직을 모아 놓은 포트이기 때문에 자기 자신의 포트를 이용해서 memberFinder를 사용할 수 있다. 조회와 변경이 분리 된 경우 이렇게 해도 됨"
- 트랜잭션 정책도 책임별로 분리:
  - 조회 서비스 → `@Transactional(readOnly = true)`
  - 변경 서비스 → `@Transactional`

# Bean Validation (메서드 파라미터 검증)
- 의존성: `spring-boot-starter-validation`
- 요청 record(`MemberRegisterRequest`)에 `@Email`, `@Size(min, max)`로 제약 선언.
- 인터페이스 메서드 파라미터에 `@Valid` 부여 → `Member register(@Valid MemberRegisterRequest req)`.
- **`@Validated`는 검증할 빈 클래스(`MemberModifyService` 등)에 붙여야 동작**한다. 테스트 클래스에 붙이면 프록시가 생성되지 않아 검증이 안 일어남.
- 검증 실패 시 `jakarta.validation.ConstraintViolationException` 발생.

# JPA 영속성 컨텍스트 & 테스트
- `application.yaml`에 `spring.jpa.show-sql: true` 설정해 실제 SQL 확인.
- 트랜잭션 안에서 변경 후 바로 조회하면 1차 캐시/dirty checking 때문에 update SQL이 로그에 안 찍힐 수 있음.
  - "영속성 컨텍스트를 비워줘야 실제 쿼리가 디비까지 이어지는지 확인할 수 있다. flush가 없으면 insert 문만 로그에 남음"
- 패턴: `entityManager.flush()` → `entityManager.clear()` 후 다시 조회해서 실제 DB 반영 검증.
- Spring Data Repository의 `save` 의미:
  - 순수 JPA만 쓰면 dirty checking으로 `save` 호출 불필요.
  - Spring Data를 쓸 때는 `save`를 호출해야 도메인 이벤트 발행(domain event publication)이 일어남.

# 용어 정리
- "가입" → "등록"으로 도메인 언어 통일 (`Member.register()`, 등록대기/등록완료, `DuplicateEmailException`).


# 오해
- JPA 모델과 도메인 모델을 분리하라는건 오해다
- 데이터 모델과 도메인 모델을 구분하는게 선호되는 경우
  - 데이터 모델과 도메인 모델이 너무 다른경우
  - 복잡한 도메인 모델이 데이터 모델과 간단히 매핑되지 않는 경우 
  - 데이터 저장 기술이 바뀌는 경우 (설득력이 있지는 않음)

# JPA란
- ORM이다.관계형 DB와 객체지향 모델의 불일치를 해결하는 기술

# 도메인 모델 패턴
- 단순 도메인 모델은 테이블과 클래스가 1:1 매핑된다.
- 복잡한 도메인 모델은 DB 매핑이 어렵다는 문제가 있다.
  - 이걸 해결해주는 것이 JPA 기술

# 스프링 데이터 프로젝트
- 다양한 데이터 저장소에 대한 데이터 접근을 단순하고 일관된 프로그래밍 모델로 제공

# equals & hash
- 원칙은 자연키로 비교하는게 원칙이나 DB 아이디로 비교하는게 좋음

# Aggregate
- 도메인 모델 구성 요소/패턴의 하나의다.
- 데이터의 변경의 목적을 위해 하나의 단위로 취급되는 연관된 객체들의 클러스터이다.
- 루트와 경계를 가진다.
- 경계 내부에는 엔티티와 값 객체가 하나 또는 여러개 존재할 수 있다.
- 데이터 변경시 하나의 단위로 취급. 데이터 변경의 일관성을 유지한다.
- 루트 엔티티에만 참조를 가질 수 있다.
- 데이터 일관성 유지. 
  - 경계내의 어떤 변경 사항이 있을때 전체 애그리거트의 모든 불변식이 충족되어야함
  - 애그리거트를 넘어서는 불변식은 이벤트나 배치등을 통해 특정 시간 내 해결 가능
- 검색 및 접근 방식: 리포지토리를 통해서 애그리거트 루트만 직접 얻을 수 있다.
  - 내부 엔티티는 루트로부터 연관관계를 통해서 접근한다.
- 일관성 유지: 객체 그룹에 적용되는 불변식을 유지하는 수단
- 이해 용이성: 객체의 시작과 끝을 명확히 해서 모델을 더 쉽게 이해하게 한다.
- 트랜잭션 및 동시성 관라: 트랜잭션 범위와 데이터 일관성 유지 방법
- 모델 단순화: 루트를 통해서만 접근
- Factory와 Repository가 복잡한 생명주기 전환을 캡슐화하는 단위

## 적용방법
- JPA의 cascading을 적절하게 활용한다. 
- 리포지토리는 애그리거트 단위로 만든다.
  - Spring Data JPA의 핵심 원칙
- 가능하다면 하나의 트랜잭션에 하나의 애그리거트만 변경한다.
- 다른 애그리거트의 참조는 애그리거트 루트에 대해서만 한다.
  - 연관관계에 있는 다른 애그리거트 루트의 에퍼런스 대신 루트의 ID 값만 저장하기도 한다.

## 애그리거트 설계 적용의 어려움
- 적절한 애그리거트 경계를 선택하는 것은 꽤 어려운 결정이다.
- 개발하면서 애그리거트의 범위가 달라지기도 한다
  - 대체로 작은 애그리거트가 될 가능성이 높다
- 성능에 부담을 주게 된다.
- 도메인 이벤트와 최종적 일관성의 사용이 요구된다.
- 완벽한 애그리거트가 아니어도 괜찮다.

## 헥사고날 아키텍처와 애그리거트
- 애그리거트 단위로 애플리케이션을 구성하는 방법이 유용하다.
- 다른 애그리게이트로의 접근은 애플리케이션 포트를 통해서 ID를 전달하는 방식으로
  - 애플리케이션 내부 리포지토리에서 루트 엔티티를 조회하는 방식으로 이루어지게 강제할 수 있다.
  - 다른 헥사고날 애플리케이션으로 구분되어진 애그리거트한테 요청을 보낼때는 아이디를 가지고 요청을 보낸다.
  - 설령 내가 객체를 참조하고 있더라도 아이디로 요청을 보내는 방법이 권장된다.
  - 포트를 통해 아이디 사용해 외부에 요청을 하더라도 디비를 조회하면 비효율적이지 않냐는 의문에는 
    JPA는 영속성 캐시를 쓰기 때문에 괜찮다라고 답변할 수 있다.
  - 도메인 이벤트와 리스터를 이용해서 애그리거트 사이의 작업을 연결할 수도 있다.
  - 애그리거트를 설계하고 각각을 독립적인 애플리케이션으로 분리하자.
  - 애그리거트 단위로 헥사고날을 구분한다

## 애그리거트 엔터티 테스트
- 루트를 통해서만 애그리거트에 속한 엔티티를 테스트해야한다.

## Request는 도메인 모델의 구성요소인가
도메인 지식을 담고 있는 값 객체이면서 애플리케이션이 제공하는 기능의 구성 정보
구성정보가 포트의 파라미터와 API 요청 정보와 일치해서 전 계층에서 재사용 
단점
- 앞의 계층에서 도메인 레벨에는 필요 없는 정보가 요구될 수 있다
- 도메인 계층이 이전 계층에 영향을 받을 수도 있다
- Request라는 이름은 도메인 모델의 지식보다 애플리케이션의 인터페이스 또는 Web API의 매핑정보에 어울리는 이름이다

## 소통 의도를 기준으로 애플리케이션 포트 구성
- 어댑터와 다른 애플리케이션 컴포넌트에서 필요한 인터페이스에만 의존할 수 있다. 
- 인터페이스에 대해 테스트를 하는게 원칙이다.
- 멀티 모듈 분리, MSA로 확장이 편리하다
- 헥사고날을 잘 하려면 애플리케이션 포트를 잘 나

## 컴포넌트
- 독립적이고 교체 가능한 모듈
- 캡슐화 되어있어야함.
- 명확한 인터페이스를 통해 협력하며 독립적으로 교체 가능한 시스템 구성 단위

## 앞으로는
- 다른 모듈과 함께 헥사고날 애플리케이션 내부의 컴포넌트로 구성
- 애그리거트와 이를 둘러싼 애플리케이션 서비스까지 하나의 애플리케이션 컴포넌트로 개발

## 애그리거트
- 애그리거트는 데이터 변경의 목적을 위해 하나의 단위로 취급되는 연관된 객체들의 집합
- 경계 내의 어떤 변경 사랑이 있을때 전체 애그리거트의 모든 불변식이 충족 되어야한다.
- 다른 애그리거트의 루트에 대한 참조를 가질 수 있다. 레퍼런스 가능
- 객체지향 패러다임과 모델의 자연스러운 매핑.
- 데이터베이스 중심의 설계로의 변질 방지
- 변경 기능은 참조를 통해서 사용하지 않도록 한다. 그러니까 조회 위주로
- 변경의 단위를 애그리거트 단위
- 애그리거트에 대한 변경은 해당 애플리케이션 컴포넌트를 통해서 진행
- 애플리케이션 컴포넌트의 인터페이스를 통해서 애그리거트를 전달할 때에는 식별자를 사용
- 컴포넌트의 의존 관계는 단방향으로. Instructor가 Member의 port만 사용하는 예