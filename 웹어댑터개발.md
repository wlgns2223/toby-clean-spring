# 애플리케이션 포트의 리턴 타입은 엔티티 vs DTO? 
- 애플리케이션 도메인 로직을 적용하고 엔티티의 정보를 돌려줄 때 어떤 타입을 사용하는가?
  - 엔티티 (애그리거트루트)
  - dto
- 애플리케이션 게층의 리턴은 타입은 DTO여야한다?
  - 계층 형 아키텍처에서 각 계층은 자기 바로 다음 계층에만 의존해야한다는 제약?
  - 여러 주장...
- 애플리케이션 계층의 리턴 타입은 가능하다면 엔티티로 해야한다는 주장
  - 완환된 계층형 아키텍처를 주로 사용한다.
  - 의존의 방향성이 중요하지 바로 다음 계층에 의존한다라는것을 강제할 필요없다.
  - 컨트롤러에 도메인 로직 유출될 수 있다.. 
    - 그러나 DTO를 가지고도 얼마든지 도메인 로직 유출이 가능하다.
    - 근본적으로 리포지토리 어댑터에서 도메인 엔티티를 제거할 방법은 없다.
  - OSIV와 결합하면 DB변경이 컨트롤러에서 일어날 수 있다.
    - 애플리케이션에서 리턴할때 트랜잭션이 종료된다.
    - 따라서 이후 엔티티 변경사항은 DB에 커밋되지 않는다.
  - Web API에서 요구하는 정보가 무엇인지, 뷰 로직에 따라 엔티티에서 복제되는 DTO의 구성이 달라진다.
  - 따라서 DTO를 통해 애플리케이션 계층이 어댑터 계층의 로직에 의존한다.
  - 이는 의존성의 방향이 어댑터로 향하게 되므로 아키텍처의 기본이 무너진다.
  - 애플리케이션은 엔티티를 돌려주고 어떤 정보를 사용자에게 전달할지 선택하는 뷰 로직은
    프레젠테이션 관심사를 가진 웹 API 어댑터가 담당한다.
  - 엔티티 단위가 아닌 복잡한 리포트성 조회 결과는 DTO를 리턴한다. 이 자체가 도메인 로직이다.
  - 개발 가이드와 코드 리뷰를 통해서 내부의 로직이 외부에 새어나가는 코드를 작성하지 않도록 주의한다.